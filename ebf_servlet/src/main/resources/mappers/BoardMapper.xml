<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ebf_servlet.mapper.BoardMapper">

    <select id="selectList" resultType="com.example.ebf_servlet.domain.BoardVO">
        select *
        from tbl_board
        order by bno
    </select>

</mapper>